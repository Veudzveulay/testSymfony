{% extends 'base.html.twig' %}

{% block title %}Dog{% endblock %}

{% block body %}
    <h1>Dog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dog.id }}</td>
            </tr>
            <tr>
                <th>Cat</th>
                <td>{{ dog.cat }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ dog.price }}</td>
            </tr>
            <tr>
                <th>Monkey</th>
                <td>{{ dog.monkey }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dog_index') }}">back to list</a>

    <a href="{{ path('dog_edit', {'id': dog.id}) }}">edit</a>

    {{ include('dog/_delete_form.html.twig') }}
{% endblock %}
